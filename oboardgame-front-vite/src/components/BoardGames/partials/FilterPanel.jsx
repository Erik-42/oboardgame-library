import React from 'react';
import { Card } from 'primereact/card';

const FilterPanel = () => {
  return (
    <Card title="Filtres">
      <p>Les options de filtrage seront bientôt disponibles ici.</p>
      {/* Contenu du panneau de filtres */}
    </Card>
  );
};

export default FilterPanel;
